[profile.default]
src = "src"
out = "out"
libs = ["lib"]

# Compiler settings
via_ir = false
optimizer = true
optimizer_runs = 1000000
evm_version = "cancun"

deny = "warnings"

[fmt]
sort_imports = true

[profile.ci]
fuzz.seed = '0'
